export function Header() {
  return (
    <header className="px-6 pt-14 pb-10 text-center">
      <h1 className="text-[22px] font-medium uppercase tracking-[0.34em] text-ink">
        Scrabble
      </h1>
      <p className="mt-2.5 font-mono text-[10px] uppercase tracking-[0.22em] text-ink-muted">
        Board Solver
      </p>
    </header>
  );
}
